import { useState } from "react";
import Header from "./Header";

const Browse = () => {

    return (
        <div>
            <Header isBrowse={true}/>
        </div>
    );
}

export default Browse;
